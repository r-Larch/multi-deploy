[Unit]
Description=Reclaim unused Docker images, containers and build cache
After=docker.service
Wants=docker.service

[Service]
Type=oneshot
User=root
WorkingDirectory=/opt/multi-deploy
ExecStart=/opt/multi-deploy/bin/docker-prune.sh

[Install]
WantedBy=multi-user.target
