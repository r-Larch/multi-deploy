# Example project definition
NAME=example
REPO=git@github.com:wedomagicai/Example.git
BRANCH=stage
COMPOSE_FILE=compose.yml
# Optional path to .env file passed to compose (not required if compose.yml references local .env)
ENV_FILE=
